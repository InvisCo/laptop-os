# Hadolint configuration for Containerfile
# https://github.com/hadolint/hadolint
ignored:
  - DL3003 # cd in subshell inside temporary multi-stage builder
  - DL3006 # Commented-out alternative FROM lines use ARG interpolation
  - DL3059 # Multiple consecutive RUN instructions — intentional design
  - DL4006 # Pipes in multi-stage builder RUN script
  - SC2312 # Consider invoking this command separately — style preference
